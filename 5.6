#include <stdio.h>
#include <stdlib.h>

/* function prototypes */
int amicable_pair(int n1, int n2);
int sumfactors(int n);

int main(int argc, char *argv[]) {
    int num1, num2;
    
    printf("Enter two integers: ");
    if (scanf("%d %d", &num1, &num2) !=2){
        printf("Invalid input\n");
        exit(EXIT_FAILURE);
    }
    if (amicable_pair(num1, num2)){
        printf("%d and %d are an amicable pair\n", num1, num2);
    }
    else {
        printf("%d and %d are not an amicable pair\n", num1, num2);
    }
    return 0;
}

int amicable_pair(int num1, int num2){
    ...
}

int sumfactors(int n){
    int sum = 1; //everything has factor 1
    int i=2;
    // search for factors stopping at sqrt(n)
    ...
    return sum;    
}
